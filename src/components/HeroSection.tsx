import React from 'react';
import { HeroGeometric } from './ui/shape-landing-hero';

const HeroSection: React.FC = () => {
  return (
    <HeroGeometric 
      badge="M30"
      title1="Transforme Sua Edição."
      title2="Impressione em Cada Projeto."
    />
  );
};

export default HeroSection;
